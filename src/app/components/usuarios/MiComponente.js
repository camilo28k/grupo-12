// m.i-app-nextjs/components/MiComponente.js
function MiComponente({ nombre, edad }) { 
  return <p>Hola, {nombre}. Tienes {edad} años.</p>; 
}

// Nota: Hay una duplicación en tu código (dos funciones iguales)
// Esta es la segunda definición idéntica:
function MiComponente({ nombre, edad }) { 
  return <p>Hola, {nombre}. Tienes {edad} años.</p>; 
}